<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:AppProjekt.ViewModels"
             xmlns:charts="clr-namespace:Microcharts.Forms;assembly=Microcharts.Forms"
             x:Class="AppProjekt.Views.HumidityPage">

    <ContentPage.Content>
        <StackLayout>
            <RefreshView x:DataType="viewmodel:HumidityViewModel" Command="{Binding GetAllMeasurements}" IsRefreshing="{Binding IsBusy, Mode=TwoWay}">
                <StackLayout>
                    <Frame BackgroundColor="AliceBlue">
                        <StackLayout>
                            <Label>Humidity %</Label>
                            <charts:ChartView x:Name="humchart" HeightRequest="300" Chart="{Binding HumidityChart}" />
                        </StackLayout>
                    </Frame>

                    <StackLayout>
                        <Button Text="10" Command="{Binding UpdateChart}" CommandParameter="50"  />
                        <Button Text="20"  Command="{Binding UpdateChart}" CommandParameter="25" />
                        <Button Text="25"  Command="{Binding UpdateChart}" CommandParameter="20" />
                    </StackLayout>
                </StackLayout>
            </RefreshView>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>